test("openProduct", "test to check if it can open a product") {
    std:base:snap:start();
    std:base:snap:prepareTest();
    std:base:products:open($product_input);
    std:base:products:closeAll();
}
